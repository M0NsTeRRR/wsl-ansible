---
- name: Install rust
  ansible.builtin.shell: |
    set -o pipefail
    curl https://sh.rustup.rs -sSf | sh -s -- -y
  args:
    executable: /bin/bash
  register: result
  changed_when: result.rc == 0
